如果想创建一个git仓库，并托管到github，那么首先需要
git init
随便创建一份文件（例如touch 1.txt）
git remote add origin https://github.com/你的用户名/仓库名.git
git add .
git commit -m "这里随便写什么都行"
git push --set-upstream origin main


这之后，每次本地仓库更新，只需要
git add .
git commit -m "这里随便写什么都行"
git push
就行了

